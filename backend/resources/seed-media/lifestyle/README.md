# Lifestyle / marketing photography

No real lifestyle photography exists yet. The homepage currently uses a
CSS-only placeholder visual (`.placeholder-visual` in
`frontend/src/styles/components.css`) instead of a photo — see that
file's usage in `HomePage.tsx` for where to swap in a real image once
one lives here.
