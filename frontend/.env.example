VITE_API_URL=http://localhost:8000/api

# Ad/analytics pixels. The production Meta Pixel uses the ID below; do not
# leave it empty when preparing the committed cPanel artifact.
# Scripts load only with the visitor's cookie consent (Meta Pixel needs the
# "marketing" category, GA4 the "analytics" one) — see src/services/analytics.ts.
VITE_META_PIXEL_ID=1544622693621669
VITE_GA4_MEASUREMENT_ID=
