# Product documents (spec sheets, certificates)

Real product PDFs go here for reference. The development dataset
attaches one placeholder spec-sheet `Media` row (on Smisul Original) —
the actual file is a genuinely valid, openable PDF generated by
`App\Support\PlaceholderMedia::minimalPdf()` at seed time, in Latin text
only (the hand-built PDF has no embedded font, so it can't render
Cyrillic — see that class's docblock). Replace with a properly authored
PDF once real documentation exists.
