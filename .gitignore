# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Environment files (root-level; backend/ and frontend/ manage their own)
.env
.env.*
!.env.example

# Logs
*.log

# playwright-cli snapshots/screenshots — debugging artifacts from browser
# automation, not source; can appear at the repo root or inside backend/
# or frontend/ depending on where the tool was invoked from.
.playwright-cli/
# Generated SuperHosting deployment artifacts
/release-superhosting/
/smisul-superhosting.zip
/composer.phar
/tmp/
/deployment/private/
# Filled-in install config with real secrets — never commit this.
/deployment/install-config.smisul.php
