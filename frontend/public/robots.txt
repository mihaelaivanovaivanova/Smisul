# Smisul storefront. Only genuinely public, indexable pages are allowed —
# authenticated/administrative/transactional flows are excluded.
User-agent: *
Disallow: /admin
Disallow: /cart
Disallow: /checkout
Disallow: /profile
Disallow: /api
Allow: /

# TODO(production): replace with the real production domain once deployed —
# see docs/legal-gdpr-seo.md. The sitemap itself is generated by the
# backend (see backend/app/Services/SitemapService.php) and reached here
# via a reverse-proxy rewrite of /sitemap.xml on this same domain.
Sitemap: http://localhost:5173/sitemap.xml
