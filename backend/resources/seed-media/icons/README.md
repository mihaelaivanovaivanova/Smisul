# Icons

The storefront's icons are hand-authored inline SVG in
`frontend/src/components/icons/Icon.tsx` — no separate icon files exist
yet. Drop a real icon set here if one is ever adopted; see that file's
docblock for how to wire it in.
